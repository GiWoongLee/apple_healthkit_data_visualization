class DataProcessor(object):

    def __init__(self,dataBundle):
        return 0
    # self.stepCountDF.index = self.stepCountDF['started_at']
    # self.stepCountDF = self.stepCountDF.drop('started_at', 1)
    # self.stepCountDF = self.stepCountDF.drop('ended_at', 1)
    # temp = self.stepCountDF.resample('H').sum().fillna(0)
    # self.stepCountDF.index = to_datetime(self.stepCountDF.index)
    # print(self.stepCountDF)
    # self.res = self.stepCountDF.resample('H').sum()
    # # print(self.stepCountDF)
    # print(self.res)
